<form method="post" action="{{ path('app_enseigner_delete', {'id': enseigner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseigner.id) }}">
    <button class="btn">Delete</button>
</form>
