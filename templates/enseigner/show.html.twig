{% extends 'base.html.twig' %}

{% block title %}Enseigner{% endblock %}

{% block body %}
    <h1>Enseigner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseigner.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enseigner_index') }}">back to list</a>

    <a href="{{ path('app_enseigner_edit', {'id': enseigner.id}) }}">edit</a>

    {{ include('enseigner/_delete_form.html.twig') }}
{% endblock %}
