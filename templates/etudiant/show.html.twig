{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>EtudiantId</th>
                <td>{{ etudiant.etudiantId }}</td>
            </tr>
            <tr>
                <th>EtudiantNom</th>
                <td>{{ etudiant.etudiantNom }}</td>
            </tr>
            <tr>
                <th>EtudiantPrenom</th>
                <td>{{ etudiant.etudiantPrenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etudiant_index') }}">back to list</a>

    <a href="{{ path('app_etudiant_edit', {'id': etudiant.id}) }}">edit</a>

    {{ include('etudiant/_delete_form.html.twig') }}
{% endblock %}
